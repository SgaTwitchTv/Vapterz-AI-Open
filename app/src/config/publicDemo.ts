/**
 * Public showcase boundary.
 *
 * The normal internal build keeps every module. `npm run build:demo` enables
 * the restricted portfolio interface without training or AI-assisted CAD.
 */
export const PUBLIC_DEMO = true;
